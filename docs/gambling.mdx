---
title: Gambling
slug: /gambling/
---

import RandomWord from "@site/src/components/RandomWord";

# GAMBLING!

Gamble for a random task this session. You're done after you complete it.

WARNING: random.

<RandomWord
  title="Gambling!"
  buttonText="gamble"
  words={[
    'Coding Tax — replace every space in a sentence with 🪙.',
    'Coding Tax II — replace every space in a sentence with 🪙🪙.',
    'Coding Tax III — replace every space in a sentence with 🪙🪙🪙.',
    'skip — do nothing. mark complete anyway.',
    'blank \'\' — make blank.py with zero lines. run it. (it does nothing. perfect.)',
    'pass — make pass.py containing only pass. run it.',
    '\'ping\' — print "ping".',
    'yawn... — print "..." three times on three lines.',
    'name? — store your name in a variable and print it.',
    'LEAVE — immediately quit with raise SystemExit.',
    'shrug — print ¯\\_(ツ)_/¯ exactly.',
    'THE UNREADABLE MEGA HYPER ULTRA FINAL INTRO BOSS OF ABSOLUTELY NOTHINGNESS 9000',
    'bahh — take input, print it with "BAHH: " in front.',
    'lower — print the input lowercased.',
    'UPPER — print the input uppercased.',
    'trim/ — print the input with leading/trailing spaces removed.',
    'doubledouble — print the input twice on one line.',
    'count! — print the number of characters in the input.',
    'Swap — replace all a with @ (or any one-letter swap).',
    'sIr lOrD cApS-a-lOtH tHe tHiRd — (prints whatever you type, but judges you)',
    '! — output every character separated by ! (e.g. h!e!l!l!o!)',
    'mirror — print the input reversed.',
    'm█S██c — print the input but replace vowels with █.',
    '"REDACTED" — replace every word with "REDACTED".',
    'WhaT — randomly uppercase some letters in the input.',
    'void — accept input, ignore it completely, print "no".',
    'beg fod — accept input, print "thx 4 {input}".',
    'mug rich person — take input, print "gimme " + input.',
    'THE POOREST CITIZEN OF THE KEYBOARD KINGDOM WHO OWNS ZERO BRACKETS AND STILL TALKS BIG — make function, but print "paid 0" (always).',
    'idle — print "...".',
    'You learn the basic taxes and fees of society — add +1 to any number no matter what.',
    'toll+ — count characters and print "toll=" + count.',
    'RECEIPT! — print the input wrapped like [RECEIPT: ...].',
    'coding tip — given bill and percent, compute tip and total.',
    'thx — print "ty" repeated n times.',
    'be grateful — if service is "good", add 10%, else 0%.',
    'wallet — store 5 numbers in a list, print sum (your wealth).',
    'price — apply a discount to input (e.g., price * 0.8).',
    'trade NOW — swap two variables (a, b).',
    'stocks — count how many items are in a list.',
    'beg for fod 2 — if input contains "pls", reduce price by 1.',
    'fod — if input contains "food", reduce price by 2.',
    'eat the soup — if input contains "warm", add "🥣 yes fod" to the output.',
    'steal — if input contains "yoink", price becomes 0.',
    'supply — if input contains "rare", price doubles, if input contains "epic", price triples, if input contains "legendary", price 100x.',
    'bait noob — if input contains "limited", add +99 (scam tax).',
    'ATTACK CODE ATTACK — power = len(attack); print "WIN" if yours > theirs.',
    'CRIT CODE ATTACK — power = len(attack), if your string contains "!!", add +5 power; print "WIN" if yours > theirs.',
    'SHEILD CODE ATTACK — power = len(attack), if string contains "try", reduce incoming by 2; print "WIN" if yours > theirs.',
    'COMBO CODE ATTACK — power = count of letters + count of digits.',
    'TRACK CODE ATTACK — power = len(attack); run 3 rounds, track wins.',
    'moody — print "😐" if input empty else "🙂".',
    'You enforce roblox law — if message contains banned word in a set, replace it with "######".',
    'tribute to god — take income, compute tax bracket (tiny if/elif ladder).',
    'this decree — generate a random "rule" from a list.',
    'draft a citizen — pick 1 random citizen name for duty.',
    'CENSUS — count vowels (population) vs consonants (army).',
    'increase army — while drafting, input citizen names and add +1 to army for each name, print final result.',
    'THE RATE-LIMITED TAX COLLECTOR BOSS (CONCURRENCY OR DEATH) — simulate collecting “tax” from 200 citizens via HTTP calls (fake/simulated ok), with retries + backoff + token-bucket rate limit + timeout handling + summary stats.',
    'EVIL BOSS — ROBLOX COMBAT HITBOX NIGHTMARE BOSS — roblox melee system with client FX but server hit validation (raycasts/region checks), cooldowns, i-frames, knockback, and anti-macro spam rules.',
    'only noob — if input contains "noob", print "WELCOME NOOB" else "WHO ARE YOU".',
    'Minecraft — replace every word with "🧱" for the length of very word (e.g: "cat" ->  "🧱🧱🧱" ).',
    'optionally expload — map ":)"->"😀", ":("->"😢", "oof"->"💥" and print.',
    'big or small tower — print floors 1..n as "floor i".',
    'THE OOF PRINCE OF ROBLOX MOUNTAIN (prints "OOF" 100 times and doesn’t crash)',
    'apperation — run git clone (any repo), then print the folder name you got.',
    'status — make a tiny change to a file, run git status, paste the output.',
    'commit — create note.txt, add 1 line, git add note.txt, commit with message "chore: note"',
    'git log — run git log -3 --oneline',
    'diff — change a line, run git diff, paste only the changed hunk.',
    'never happen — make a change, then undo it with git restore',
    'ignore — add .venv/ to .gitignore, confirm it stops showing in git status.',
    'prmsg — write a pretend PR title + 3 bullet description in PR.md (no actual PR needed).',
    'release — create CHANGELOG.md with one entry for v0.0.1 (even if fake).',
    'TESTTESTTEST git — add a minimal .github/workflows/ci.yml that runs pytest (can be copy/paste), commit it.',
    'THE MERGE CONFLICT GREMLIN OF DOOM (INTENTIONALLY BREAK IT, THEN FIX IT) — create two branches that edit the same line, merge to trigger a conflict, resolve it cleanly, and show the final git log --graph --oneline --decorate -5.',
    'guess — “Guess the number” (1–10). loop until correct, print attempts',
    'quiz — 5-question quiz (hardcoded Q/A), score at the end.',
    'gamble — 3-symbol slot machine (🍒🍋⭐), payout rules, play until coins = 0.',
    'rps — create rock/paper/scissors vs computer (best of 3).',
    'rng — flip a coin 10 times, track heads/tails score, declare winner in print.',
    'DRAGON I HATCHLING OF HELLO FIRE — print an ASCII dragon + the line "i breathe fire".',
    'DRAGON II — SCALES-TAX COLLECTOR — "Dragon Tax": replace every ch with "🐉", but leave whitespaces.',
    'DRAGON III — HOARD COUNTER — given a list of coin values, print total hoard.',
    'DRAGON IV — THE VOWEL INCINERATOR — replace every vowel in input with "🔥", at the end, print "BURNED AEIOU HAHAHA".',
    'DRAGON V — THE NAME FORGE — randomly generate a dragon name from 3 lists (prefix/syllable/title).',
    'DRAGON VI — LAIR MAP WALKER — 3×3 grid text maze; input N/E/S/W; win when you reach the "dragon treasure".',
    'DRAGON VII — DUEL OF CLAWS (BEST OF 5) — rock/paper/scissors reskin: claw/wings/fire, but the dragon (computer) always uses fire. track score.',
  ]}
/>
